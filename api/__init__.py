"""
API Layer — FastAPI REST Server for AI Resume Matcher.

Exposes the matching engine to the UI server via HTTP endpoints.
Handles file uploads, async batch processing, and client-scoped matching.
"""
